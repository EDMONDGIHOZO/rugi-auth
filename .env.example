########################################
# Rugi Auth - Example Environment File #
########################################

# NODE ENVIRONMENT
# One of: development, production, test
NODE_ENV=development

# HTTP SERVER
PORT=3000

########################################
# DATABASE CONFIG                      #
########################################

# Application database connection (Prisma)
# Update user/password/host/db as needed
DATABASE_URL="postgresql://rugi:rugi_password@localhost:5432/rugi_auth?schema=public"

# PostgreSQL container configuration (used by docker-compose.yml)
POSTGRES_USER=rugi
POSTGRES_PASSWORD=rugi_password
POSTGRES_DB=rugi_auth

########################################
# JWT / AUTH CONFIG                    #
########################################

# JWT issuer (appears as `iss` in tokens)
JWT_ISSUER="rugi-auth"

# Access token lifetime
JWT_ACCESS_TOKEN_EXPIRY=10m

# Refresh token lifetime
JWT_REFRESH_TOKEN_EXPIRY=7d

########################################
# KEY PATHS                            #
########################################

# Paths to RSA keys (generated with `npm run generate:keys`)
PRIVATE_KEY_PATH=./keys/private.pem
PUBLIC_KEY_PATH=./keys/public.pem

########################################
# CORS / SECURITY                      #
########################################

# Comma-separated list of allowed origins
CORS_ORIGIN="http://localhost:3000,http://localhost:3001,http://localhost:5173"

# Rate limiting configuration
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Logging level: fatal | error | warn | info | debug | trace
LOG_LEVEL=info

########################################
# EMAIL (SMTP)                         #
########################################

# Optional SMTP configuration for sending emails
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME="Rugi Auth"

########################################
# FRONTEND + TOKEN CONFIG              #
########################################

# Base URL for frontend app (used in email links)
FRONTEND_URL="http://localhost:3000"

# Password reset token expiry
PASSWORD_RESET_TOKEN_EXPIRY=1h

# OTP configuration
OTP_EXPIRY=10m
OTP_LENGTH=6